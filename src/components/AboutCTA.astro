---
const base = import.meta.env.BASE_URL;
const { href = `${base}/book`, label = 'Book a Free Consultation' } = Astro.props;
---
<div class="mt-12">
  <a href={href} class="btn-primary !text-white no-underline">{label}</a>
</div>
