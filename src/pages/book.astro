---
import Layout from '../components/Layout.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import { frontmatter } from '../content/pages/book.md';
import MockScheduler from '../components/MockScheduler.astro';
const { title, intro } = frontmatter || {};
---
<Layout>
  <Header slot="header" />

  <section>
    <h1 class="font-heading mb-6">{title ?? 'Book a Free Consultation'}</h1>
    <p class="text-lg text-slate leading-relaxed max-w-2xl">{intro ?? 'Schedule a free consultation — pick a time that works for you.'}</p>
    <div class="mt-12">
      <MockScheduler />
    </div>
  </section>

  <Footer slot="footer" />
</Layout>
